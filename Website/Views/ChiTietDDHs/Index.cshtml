@model IEnumerable<Website.Models.ChiTietDDH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DDH.MaKhachHang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VatTu.TenVatTu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoLuong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonGia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DDH.MaKhachHang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VatTu.TenVatTu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoLuong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonGia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDChiTiet }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDChiTiet }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDChiTiet })
        </td>
    </tr>
}

</table>
